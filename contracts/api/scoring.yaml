openapi: 3.0.3
info:
  title: Poise Scoring API
  version: 0.0.0
  description: >
    Stub contract — owned by the developer assigned to this phase. Define
    the real paths/schemas here before writing the corresponding router in
    backend/app/routers/scoring.py, so parallel phases can code against this
    file with mock data (see contracts/mocks/) instead of a running server.
paths: {}
